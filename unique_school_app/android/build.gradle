// Top-level build file for all sub-projects/modules.
// Plugin versions are managed in settings.gradle via pluginManagement.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
